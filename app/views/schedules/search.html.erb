<h1>
  検索結果
</h1>
<%# 検索結果の個数で条件分岐 %>
<% if @results.length !=0 %>
  <% @results.each do |result| %>
    <td>
    <br>
    <li>
    <%= "出発空港：#{result.departure.departure}" %>
    <%= "到着国:#{result.country}" %>
    <%= "航空会社：#{result.airline}" %>
    <%= "便名:#{result.fltnum}" %>
    <%= "出発時刻:#{result.dptime.strftime('%H:%M')}" %>
    <%= "到着時刻：#{result.artime.strftime('%H:%M')}" %>
    <%= "曜日：#{result.day}" %>
    </li>
  <% end %>
<% else %>
  該当する商品はありません
<% end %>
<br>
<%= link_to 'トップページへ戻る', root_path %>